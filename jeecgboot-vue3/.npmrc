# 说明：以下键为 npm 无法识别的专用配置，删除以避免 npm 的 Unknown project config 警告。
# 1) shamefully-hoist / strict-peer-dependencies：pnpm 专用；若重新改用 pnpm 安装依赖，请恢复这两行。
# 2) electron_mirror / electron_builder_binaries_mirror：Electron 二进制镜像；当前项目无 electron 依赖，暂不需要。
